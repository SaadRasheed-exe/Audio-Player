#pragma once
#include <iostream>

// Takes a 2 numbers and returns true is the first number is a power of the second.
bool isPowerOf(int, int);

// Converts all characters in a string to lower case and returns the new string.
std::string tolower(std::string);

bool check(char name[261]);

void refresh();